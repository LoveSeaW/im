version: '3'
services:

  postgres:
    image: postgres:17
    container_name: im_postgres
    restart: always
    network_mode: host
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: fim_server_db
    volumes:
      - ./postgres/datadir:/var/lib/postgresql/data
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql

  redis:
    image: redis:7.4-alpine
    container_name: im_redis
    restart: always
    network_mode: host

  etcd:
    image: bitnami/etcd:3.5.19
    container_name: im_etcd
    network_mode: host
    environment:
      - ALLOW_NONE_AUTHENTICATION=yes
      - ETCD_ADVERTISE_CLIENT_URLS=http://127.0.0.1:2379
      - ETCD_LISTEN_CLIENT_URLS=http://0.0.0.0:2379

  zookeeper:
    image: bitnami/zookeeper:3.9
    container_name: im_zookeeper
    restart: always
    network_mode: host
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes
      - ZOO_ENABLE_ADMIN_SERVER=no

  kafka:
    image: bitnami/kafka:3.9
    container_name: im_kafka
    restart: always
    network_mode: host
    environment:
      - ALLOW_PLAINTEXT_LISTENER=yes
      - KAFKA_CFG_ZOOKEEPER_CONNECT=127.0.0.1:2181
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://127.0.0.1:9092
    depends_on:
      - zookeeper

  kafka-map:
    image: dushixiang/kafka-map:latest
    container_name: im_kafka_map
    restart: always
    network_mode: host
    environment:
      - DEFAULT_USERNAME=admin
      - DEFAULT_PASSWORD=admin
      - SERVER_PORT=9001

  gateway:
    build:
      context: fim_gateway
    container_name: im_gateway
    restart: always
    network_mode: host
    volumes:
      - ./fim_gateway/gateway.yaml:/app/gateway.yaml

  auth_api:
    build:
      context: fim_auth/auth_api
    container_name: im_auth_api
    restart: always
    network_mode: host
    volumes:
      - ./fim_auth/auth_api/auth.yaml:/app/auth.yaml
    depends_on:
      - postgres
      - redis
      - user_rpc
      - settings_api
      - kafka

  chat_api:
    build:
      context: fim_chat/chat_api
    container_name: im_chat_api
    restart: always
    network_mode: host
    volumes:
      - ./fim_chat/chat_api/chat.yaml:/app/chat.yaml
    depends_on:
      - postgres
      - redis
      - user_rpc
      - file_rpc

  chat_rpc:
    build:
      context: fim_chat/chat_rpc
    container_name: im_chat_rpc
    restart: always
    network_mode: host
    volumes:
      - ./fim_chat/chat_rpc/chatrpc.yaml:/app/chatrpc.yaml
    depends_on:
      - postgres

  file_api:
    build:
      context: fim_file/file_api
    container_name: im_file_api
    restart: always
    network_mode: host
    volumes:
      - ./fim_file/file_api/file.yaml:/app/file.yaml
      - ./fim_file/file_api/uploads:/app/uploads
    depends_on:
      - postgres
      - user_rpc

  file_rpc:
    build:
      context: fim_file/file_rpc
    container_name: im_file_rpc
    restart: always
    network_mode: host
    volumes:
      - ./fim_file/file_rpc/filerpc.yaml:/app/filerpc.yaml
    depends_on:
      - postgres

  group_api:
    build:
      context: fim_group/group_api
    container_name: im_group_api
    restart: always
    network_mode: host
    volumes:
      - ./fim_group/group_api/group.yaml:/app/group.yaml
    depends_on:
      - postgres
      - redis
      - user_rpc
      - group_rpc

  group_rpc:
    build:
      context: fim_group/group_rpc
    container_name: im_group_rpc
    restart: always
    network_mode: host
    volumes:
      - ./fim_group/group_rpc/grouprpc.yaml:/app/grouprpc.yaml
    depends_on:
      - postgres
      - redis

  logs_api:
    build:
      context: fim_logs/logs_api
    container_name: im_logs_api
    restart: always
    network_mode: host
    volumes:
      - ./fim_logs/logs_api/logs.yaml:/app/logs.yaml
    depends_on:
      - postgres
      - user_rpc
      - kafka

  settings_api:
    build:
      context: fim_settings/settings_api
    container_name: im_settings_api
    restart: always
    network_mode: host
    volumes:
      - ./fim_settings/settings_api/settings.yaml:/app/settings.yaml
    depends_on:
      - postgres

  settings_rpc:
    build:
      context: fim_settings/settings_rpc
    container_name: im_settings_rpc
    restart: always
    network_mode: host
    volumes:
      - ./fim_settings/settings_rpc/settingsrpc.yaml:/app/settingsrpc.yaml
    depends_on:
      - postgres

  user_api:
    build:
      context: fim_user/user_api
    container_name: im_user_api
    restart: always
    network_mode: host
    volumes:
      - ./fim_user/user_api/users.yaml:/app/users.yaml
    depends_on:
      - postgres
      - redis
      - user_rpc
      - chat_rpc
      - group_rpc

  user_rpc:
    build:
      context: fim_user/user_rpc
    container_name: im_user_rpc
    restart: always
    network_mode: host
    volumes:
      - ./fim_user/user_rpc/userrpc.yaml:/app/userrpc.yaml
    depends_on:
      - postgres
      - redis

  fim_web:
    image: nginx:alpine
    container_name: im_fim_web
    restart: always
    network_mode: host
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/logs:/var/log/nginx/
      - ./fim_web/dist:/usr/share/nginx/fim_web
      - ./nginx/cert/:/usr/share/nginx/cert
